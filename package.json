{
  "name": "till-debt-do-us-part",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "capture": "node scripts/capture-screenshots.mjs",
    "check:cycles": "madge --circular --extensions ts src",
    "test:e2e": "node tests/qa-tile-kit-regression.mjs",
    "test:prod-gate": "npm run build && node scripts/check-prod-bundle.mjs"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "@types/three": "^0.160.0",
    "canvas": "^3.2.3",
    "madge": "^8.0.0",
    "puppeteer-core": "^25.4.0",
    "typescript": "^5.9.3",
    "vite": "^4.5.0"
  }
}
